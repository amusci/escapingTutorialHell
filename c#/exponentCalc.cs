using System;


    public static long СalculateExponent(long number, long exponent) 
    /* Create a function that takes a base number and an exponent number and returns the calculation. */
    {
      return (long)Math.Pow(number, exponent);

    }